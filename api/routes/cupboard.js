const express = require("express");

const CupboardController = require("");

const router = express.Router();

//TBC not sure what we need in regards to the cupboard component

module.exports = router;